name: CI/CD pipeline for Backend Service

on:
  push:
    branches: [ main, "dev/**", "feature/**"]

env:
  SERVICE_NAME: jobhuntly-backend
  DEPLOY_DIR: ~/jobhuntly-service
  DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
  DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
  VPS_HOST: ${{ secrets.VPS_HOST }}
  VPS_USER: ${{ secrets.VPS_USER }}
  VPS_SSH_KEY: ${{ secrets.VPS_SSH_KEY }}
  DOCKER_IMAGE: job-huntly-backend
  DOCKER_TAG: latest

jobs:
  build:
    name: Build & Push Docker image
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: "17"
          distribution: "temurin"

      - name: Set up Docker BuildX
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ env.DOCKERHUB_USERNAME }}
          password: ${{ env.DOCKERHUB_TOKEN }}

      - name: Build and Push Docker Image
        run: |
          docker build --no-cache -t $DOCKER_IMAGE:$DOCKER_TAG .
          docker push $DOCKER_IMAGE:$DOCKER_TAG

      # Copy necessary files to server
      - name: Copy docker-compose & env file to VPS
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ env.VPS_HOST }}
          username: ${{ env.VPS_USER }}
          key: ${{ env.VPS_SSH_KEY }}
          source: "docker-compose.yml"
          target: ${{ env.DEPLOY_DIR }}

  deploy:
    name: Deploy Backend Service
    runs-on: ubuntu-latest
    needs: [ build ]

    steps:
      - name: Deploy on VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ env.VPS_HOST }}
          username: ${{ env.VPS_USER }}
          key: ${{ env.VPS_SSH_KEY }}
          script: |
            set -euo pipefail

            echo "➡️  Di chuyển tới thư mục triển khai..."
            mkdir -p ${{ env.DEPLOY_DIR }}
            cd ${{ env.DEPLOY_DIR }}

            echo "📄  Bảo đảm có file env rỗng..."
            touch .env.local.properties

            echo "🔗  Tạo network (nếu chưa có)..."
            docker network create customer-network || true

            echo "🔑  Docker login (nếu image private)..."
            echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login -u "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin || true

            echo "🛑  Dừng container cũ (nếu tồn tại)..."
            docker-compose -f docker-compose.yml stop ${{ env.SERVICE_NAME }} || true
            docker-compose -f docker-compose.yml rm -f ${{ env.SERVICE_NAME }} || true

            echo "⬇️  Kéo image mới nhất..."
            docker-compose -f docker-compose.yml pull ${{ env.SERVICE_NAME }} || true

            echo "🚀  Khởi chạy container mới..."
            docker-compose -f docker-compose.yml up -d --remove-orphans

            echo "🧹  Dọn rác image cũ..."
            docker image prune -f

            echo "✅  Triển khai hoàn tất!"